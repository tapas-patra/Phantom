<Window x:Class="SecureOverlay.ScreenshotCapture"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        WindowStyle="None"
        ResizeMode="NoResize"
        Topmost="True"
        AllowsTransparency="True"
        Background="#80000000"
        WindowState="Maximized"
        ShowInTaskbar="False"
        ShowActivated="True"
        Cursor="Cross"
        ForceCursor="True"
        PreviewKeyDown="Window_KeyDown"
        KeyDown="Window_KeyDown">
    
    <Grid>
        <Border Background="#DD000000" 
                VerticalAlignment="Top" 
                HorizontalAlignment="Center" 
                Margin="0,20,0,0" 
                Padding="20,10"
                CornerRadius="8"
                IsHitTestVisible="False">
            <StackPanel>
                <TextBlock Text="Select a region" 
                          FontSize="18" 
                          FontWeight="Bold" 
                          Foreground="White" 
                          HorizontalAlignment="Center"/>
                <TextBlock Text="Click and drag to select. ENTER = full screen  |  ESC = cancel" 
                          FontSize="12" 
                          Foreground="#CCFFFFFF" 
                          HorizontalAlignment="Center" 
                          Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <Canvas x:Name="SelectionCanvas" Background="Transparent">
            <Rectangle x:Name="SelectionRectangle" 
                      Stroke="#00A8FF" 
                      StrokeThickness="2" 
                      Fill="#3300A8FF" 
                      Visibility="Collapsed"
                      IsHitTestVisible="False"/>
        </Canvas>
    </Grid>
</Window>
