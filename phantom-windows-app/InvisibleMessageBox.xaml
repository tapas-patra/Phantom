<Window x:Class="SecureOverlay.InvisibleMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" Height="200" Width="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        Cursor="Arrow"
        ForceCursor="True">
    
    <Window.Resources>
        <DropShadowEffect x:Key="Shadow" ShadowDepth="3" BlurRadius="10" Opacity="1" Color="Black"/>
    </Window.Resources>

    <Grid>
        <Border Background="#70000000" CornerRadius="12" BorderBrush="#90FFFFFF" BorderThickness="2">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="30" Opacity="0.9" Color="Black"/>
            </Border.Effect>
            
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="TitleText" Grid.Row="0" Text="Message" 
                          FontSize="16" FontWeight="Bold" Foreground="White"
                          Margin="0,0,0,15" Effect="{StaticResource Shadow}"/>

                <TextBlock x:Name="MessageText" Grid.Row="1" Text="Message content" 
                          FontSize="14" Foreground="White" TextWrapping="Wrap"
                          Effect="{StaticResource Shadow}"/>

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button x:Name="OkButton" Content="OK" Width="80" Height="30"
                           Click="OkButton_Click" Margin="0,0,10,0"
                           Background="#5000AA00" BorderBrush="#9000AA00" BorderThickness="1.5"
                           Foreground="White" FontWeight="Bold" Cursor="Hand"/>
                    <Button x:Name="CancelButton" Content="Cancel" Width="80" Height="30"
                           Click="CancelButton_Click" Visibility="Collapsed"
                           Background="#50AA0000" BorderBrush="#90AA0000" BorderThickness="1.5"
                           Foreground="White" FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
